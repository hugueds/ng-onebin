import { PopActiveDirective } from './pop-active.directive';

describe('PopActiveDirective', () => {
  it('should create an instance', () => {
    const directive = new PopActiveDirective();
    expect(directive).toBeTruthy();
  });
});
